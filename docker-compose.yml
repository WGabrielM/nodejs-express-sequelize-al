version: '3.9'

services:
  postgres:
    container_name: nodejs-express-sequelize-al
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=express-sequelize